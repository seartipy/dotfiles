{
 :layers
 [
  :core
  :tools/git
  :tools/linter
  :tools/terminal
  :tools/build

  :lang/clojure
  :lang/typescript
  :lang/html
  :lang/css
  :lang/json

  ;; :tools/bookmarks
  ;; :tools/todo
  ;; :tools/minimap
  ;; :lang/markdown

  ;; :lang/haskell
  ; :lang/python
  ;; :lang/ruby
  ;; :lang/elm
  ;; :lang/javascript
  ;; :lang/less
  ;; :lang/sass
  ;; :lang/jade
  ;; :lang/stylus
  ;; :lang/rust
  ;; :lang/csharp
  ;; :lang/elixir
  ;; :lang/go
  ;; :langjulia
  ;; :lang/swift
  ]

 :additional-packages
 [
  :hyperclick
  :jumpy
  :last-cursor-position


  :editorconfig

  :js-hyperclick
  :linter-eslint
  :language-babel
  ;; :nuclide

  ;;:browser-plus ; for livereload
  ;; :sublime-style-column-selection
  ;; :fonts
  ;; :script
  ;; :toggle-quotes
  ;; :floobits
  ;; :disable-keybindings
  ;; :narrow
  ;; :choose-pane

  ;; :language-scala
  ;; :intentions

  ;; :javascript-snippets
  ;; :react-snippets

  ;; :ide-haskell-repl

  ;; :clang-format
  ;; :linter-clang
  ;; :autocomplete-clang

  ;; :git-time-machine
  ;; :git-log
  ;; :split-diff

  ;; :hydrogen

  ;; :proto-repl-charts
  ;; :lisp-paredit
  ;; :tool-bar
  ;; :set-syntax
  ;; :mdn-doc
 ]

 :configuration
 [
  ["editor.fontSize" "13"]
  ["editor.fontFamily" "monaco, consolas, ubuntu mono, mononoki, hack, source code pro, monospace"]
  ["core.themes" ["one-dark-ui" "one-dark-syntax"]]
  ["proton.core.showTabBar" true]
  ["editor.scrollPastEnd" true]
  ["proton.core.relativeLineNumbers" true]

  ;; :vim-mode-plus, :emacs :default available
  ["proton.core.inputProvider" :emacs]
  ;; ["linter-eslint.useGlobalEslint" true]
  ["disable-keybindings.communityPackages" ["emmet"]]

  ;; ["tool-bar.visible" false]
  ;; ["proton.core.modeKey" "enter"]

  ;; ["proton.core.wipeUserConfigs" false]
  ["nuclide.nuclide-flow.canUseFlowBin" true]
  ["nuclide.nuclide-home.showHome" false]
 ]

 :keybindings {}

 :keymaps [{:selector ".tree-view" :keymap [["escape" "tree-view:toggle"]]}
           ;; {:selector "atom-workspace:not([mini])" :keymap [["ctrl-x ctrl-o" "choose-pane:start"]]}
           {:selector "atom-text-editor" :keymap [["alt-space s a" "narrow:lines"]]}
           ;; {:selector "atom-text-editor.vim-mode-plus.insert-mode" :keymap [["f d" "vim-mode-plus:activate-normal-mode"]]}
           ;; {:selector "atom-text-editor.vim-mode-plus.insert-mode.autocomplete-active" :keymap [["escape" "autocomplete-plus:cancel"]]}
           ]
 }
